# pending/

Landing zone for BMAD-prep summaries that should be synced into a BMAD project.

When you run `.\scripts\mini-bmad.ps1 bmadprep-sync -Project "<slug>"`, all files in this folder are copied into the project’s `bmad-projects/<slug>/_mini-bmad-updates/` and then moved to `../done_prep_bmad/`.

